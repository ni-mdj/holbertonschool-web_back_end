// Retourne tjrs une Promise (vide) pour vérifier que c'est bien une instance de Promise
export default function getResponseFromAPI() {
    return new Promise((resolve, reject) => {
      resolve();
    });
  }